<?xml version="1.0" encoding="utf-8"?>
<!-- MarioRS Android 清单文件 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- OpenGL ES 3.0 要求 (wgpu GLES 后端需要) -->
    <!-- 注意: 设置 required="false" 允许在不支持的设备上安装，运行时检测 -->
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:hasCode="true"
        android:allowBackup="true">

        <!-- 使用自定义 MainActivity, 继承 GameActivity, 添加原生按钮覆盖层 -->
        <activity
            android:name="com.mariogame.mario.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="landscape"
            android:theme="@style/GameActivityTheme"
            android:exported="true">

            <!-- 指定 Rust 编译的动态库名称 -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="mario" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
